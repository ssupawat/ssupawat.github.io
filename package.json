{
  "private": true,
  "description": "A simple static blog built with vanilla JavaScript",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && echo 'Open dist/index.html in your browser'"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
